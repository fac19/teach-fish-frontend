import styled from "styled-components";

const RadioButtonFieldset = styled.fieldset`
  display: flex;
  flex-direction: column;
  & > * {
    padding: 1.5rem 0 1.5rem 0;
  }
`;

export default RadioButtonFieldset;
