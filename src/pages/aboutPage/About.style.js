import styled from "styled-components";

const Text = styled.p`
  text-align: center;
`;
const AboutWrapper = styled.section`
  display: flex;
  flex-direction: column;
`;

export { Text, AboutWrapper };
